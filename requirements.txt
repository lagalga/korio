# Core
python-dotenv>=1.0.0
requests>=2.31.0

# Base de datos
supabase>=2.3.4
psycopg2-binary>=2.9.9

# Chunking de texto
langchain>=0.1.9
langchain-community>=0.0.26
langchain-text-splitters>=0.0.1

# Procesamiento vectorial
numpy>=1.26.0

# Procesamiento de documentos
markitdown[pdf,docx,xlsx,pptx]>=0.1.0
pymupdf>=1.25.0

# Anonimización PII
presidio-analyzer>=2.2.351
presidio-anonymizer>=2.2.351
spacy>=3.7.0

# API REST
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
email-validator>=2.0.0
falkordb>=1.0.10
python-multipart>=0.0.9

# MCP Server (Phase 7.3) — expone Korio como servidor MCP HTTP/SSE
mcp>=1.2.0

# Observabilidad (sesión 18) — trazas distribuidas del pipeline RAG en LangSmith.
# Degrada a no-op si LANGCHAIN_TRACING_V2 != true (ver src/observability.py).
langsmith>=0.8.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1

# Utilidades
pyyaml>=6.0.1

# Dev tooling: pre-commit hooks (gitleaks + detect-secrets + check-large-files).
# Tras `pip install -r requirements.txt`, ejecutar `pre-commit install` una vez
# para activar los hooks locales. Ver `.pre-commit-config.yaml`.
pre-commit>=4.0.0
detect-secrets>=1.5.0
